package orchestration

const default_inventory = "inventory"
const default_ssh_config = "ssh.cfg"
const default_ssh_key = "ssh.key"
const default_terraform_state = "terraform.tfstate"
const default_terraform_config = "configuration.tfvars"